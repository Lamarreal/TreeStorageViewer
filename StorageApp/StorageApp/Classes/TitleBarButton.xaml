<UserControl x:Class="StorageApp.Classes.TitleBarButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:StorageApp.Classes"
             mc:Ignorable="d"
             d:DataContext="{Binding RelativeSource={RelativeSource Self}}"
             d:DesignWidth="150" d:DesignHeight="36">
    
    <Border x:Name="Bg" CornerRadius="6" Padding="10,2,10,2" Cursor="Hand"
            MouseEnter="Bg_MouseEnter"
            MouseLeave="Bg_MouseLeave"
            MouseDown="Bg_MouseDown">
       
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="Transparent"/>
                
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsHighlighted, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                        <Setter Property="Background" Value="{StaticResource SelectionColor}"/>
                        <Setter Property="TextElement.Foreground" Value="White"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
       
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            
            <TextBlock x:Name="BtnLabel" 
                       Foreground="{StaticResource TextColor}" 
                       Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                       d:Text="Designer Button Title"
                       VerticalAlignment="Center">
                <TextBlock.Effect>
                    <DropShadowEffect BlurRadius="5"  ShadowDepth="2" Opacity="0.4"/>
                </TextBlock.Effect>
            </TextBlock>
            
            <Image Source="/Images/ArrowDown.png" 
                   Height="10" Width="10" 
                   RenderOptions.BitmapScalingMode="HighQuality" 
                   Margin="10,2,0,0">
                <Image.Style>
                    <Style TargetType="Image">
                        <Setter Property="Visibility" Value="Collapsed"/>
            
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasDropdown, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Image.Style>
            </Image>
        </StackPanel>
    </Border>
</UserControl>